@model List<HowItWork>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h2>Nasıl Çalışır Listesi</h2>
            <a href="/HowItWork/Create" class="btn btn-success mb-3">
                <i class="fa fa-plus"></i> Yeni Ekle
            </a>
            <table class="table table-bordered table-hover table-striped">
                <thead class="table-dark">
                    <tr>
                        <th>Id</th>
                        <th>Adım No</th>
                        <th>Başlık</th>
                        <th>Açıklama</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.OrderBy(x => x.StepNumber))
                    {
                        <tr>
                            <td>@item.HowItWorkId</td>
                            <td>@item.StepNumber</td>
                            <td>@item.Title</td>
                            <td>@item.Description</td>
                            <td>
                                <div class="d-flex gap-2">
                                    <a href="/HowItWork/Update/@item.HowItWorkId" class="btn btn-warning btn-sm">
                                        <i class="fa fa-pencil"></i> Düzenle
                                    </a>
                                    <a href="/HowItWork/Delete/@item.HowItWorkId" class="btn btn-danger btn-sm"
                                       onclick="return confirm('Silmek istediğinize emin misiniz?')">
                                        <i class="fa fa-trash"></i> Sil
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

