@model List<Adventure>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h2>Adventure Listesi</h2>
            <a href="/Adventure/Create" class="btn btn-success mb-3">
                <i class="fa fa-plus"></i> Yeni Ekle
            </a>
            <table class="table table-bordered table-hover table-striped">
                <thead class="table-dark">
                    <tr>
                        <th>Id</th>
                        <th>İkon</th>
                        <th>Başlık</th>
                        <th>Açıklama</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.AdventureId</td>
                            <td><i class="fa @item.Icon"></i> @item.Icon</td>
                            <td>@item.Title</td>
                            <td>@item.Description</td>
                            <td>
                                <div class="d-flex gap-2">
                                    <a href="/Adventure/Update/@item.AdventureId" class="btn btn-warning btn-sm">
                                        <i class="fa fa-pencil"></i> Düzenle
                                    </a>
                                    <a href="/Adventure/Delete/@item.AdventureId" class="btn btn-danger btn-sm"
                                       onclick="return confirm('Silmek istediğinize emin misiniz?')">
                                        <i class="fa fa-trash"></i> Sil
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

